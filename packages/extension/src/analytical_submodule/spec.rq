spec this folder is the extension host adapter for `@reqlan/analytical`: VS Code wiring (watchers, Uri helpers, IndexService) over the shared WorkspaceIndex engine. Index ownership lives in packages/analytical. Product-surface registration lives in ["../extension/register-contributions.ts"]. Export UI lives in ["../export_module"].
