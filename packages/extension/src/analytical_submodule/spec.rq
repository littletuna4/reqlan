spec this folder is the extension host adapter for `@reqlan/analytical`: VS Code wiring (watchers, Uri helpers, export UI, command registration) over the shared WorkspaceIndex engine. Index ownership lives in packages/analytical.
