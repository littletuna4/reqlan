{
    "name": "reqlan-cli",
    "description": "The cli specific package",
    "version": "0.0.1",
    "type": "module",
    "engines": {
        "node": ">=20.10.0",
        "npm": ">=10.2.3"
    },
    "files": [
        "bin",
        "out",
        "src"
    ],
    "bin": {
        "reqlan-cli": "./bin/cli.js"
    },
    "scripts": {
        "clean": "shx rm -fr *.tsbuildinfo out",
        "build": "echo 'No build step'",
        "build:clean": "pnpm run clean && pnpm run build"
    },
    "dependencies": {
        "reqlan-language": "workspace:*",
        "chalk": "~5.6.2",
        "commander": "~15.0.0"
    }
}
