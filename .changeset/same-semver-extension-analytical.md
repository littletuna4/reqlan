---
"@reqlan/analytical": patch
"reqlan-extension": patch
---

Keep the VS Code extension on the same semver as `@reqlan/analytical` and its platform native packages. Marketplace already published 1.10.x, so the shared number stays on that line rather than 0.10.x.
