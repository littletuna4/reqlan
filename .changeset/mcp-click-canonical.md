---
"@reqlan/mcp": minor
---

Make `click` the only MCP search and retrieval tool. Remove parse, search, list, file_context, local_graph, summarize_subtree, requirement_reference, file_reference, and list_interactions from the MCP surface.
