[package]
name = "reqlan-parse"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Context-sensitive lexer and recursive-descent parser for reqlan (.rq) index-path documents"

[dependencies]
thiserror.workspace = true

[dev-dependencies]
