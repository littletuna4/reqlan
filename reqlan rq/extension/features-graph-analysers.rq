from "./features-graph.rq" import graph_design
from "./module/index.rq" import application_memory
import "../../packages/language/src/reqlan-comment.langium" as comment_grammar
import "../../packages/analytical/src/core/application-memory.ts" as application_memory_impl
import "../../packages/analytical/src/native/native-analysis-api.ts" as analysis_runtime
import "../../packages/analytical/src/index-store/workspace-index.ts" as workspace_index
import "../../packages/extension/src/analytical_submodule/index-store/index-service.ts" as editor_index_adapter

index_ideas {
    Each ["../bases/base.rq".base] maintains an index of its ideas for fast lookup and navigation (["../indexer/indexer.rq".index]).
    The index should update incrementally as .rq files change within that base (nested child bases excluded).
    Persistence lives in [application_memory] per base, shared with CLI and MCP — not VS Code extension storage.
    Engine ownership: [workspace_index] in `@reqlan/analytical`; multi-base via BaseRegistry; editor watches via [editor_index_adapter].
}

index_technology {
    The [index_ideas] graph database is rusqlite via the core native engine, aligned with [graph_design].
    On-disk location is [application_memory] (`<base>/.reqlan/ideas-index.sqlite`), not `context.globalStorageUri`.
    Implementation: [application_memory_impl], [workspace_index], headless [analysis_runtime] for CLI/MCP, and [editor_index_adapter] for the VS Code host.
}

indexing_incrementality {
    Indexing should be incremental: watcher-driven single-file updates, and soft full syncs that skip unchanged files by stored mtime.
    Hard rebuilds reparse everything. Detail and UX (progress, cancel, non-blocking UI): ["../indexer/indexer.rq".nonblocking_index].
}

indexing_trigger_filesave indexing should be triggered by .rq file create/change/delete via watchers, and by allowlisted source files that may contain comment references.
indexing_trigger_manual indexing should be triggered by manual indexing commands (Refresh / Clear & rebuild).

indexing_trigger_open {
    After the activity bar's first painted frame / base activate, run one soft sync (mtime-skipped) so the index is warm without delaying the sidebar shell.
    This is a startup pass — not the idle checker ([indexing_trigger_auto]).
}

indexing_trigger_auto {
    When the editor host is idle, run a **background staleness check** — not a reindex and not a soft full sync UI pass.
    Philosophy: computationally cheap. Load document mtimes in **one** SQLite read, compare each file's FS mtime in memory, and only then index mismatched or new files; drop deleted docs in one batched delete.
    A single MAX(mtime) watermark is not sufficient (one file can get newer while another gets older).
    If nothing is stale: no state transition, no progress UI, no parse.
    If some files are stale: index **only those files** under ["../indexer/indexer.rq".nonblocking_index].
    Schedule after a quiet period with no index activity; prefer running when the window is unfocused; defer or cancel if the user becomes active again.
    Extension host only ([editor_index_adapter]); CLI/MCP have no idle loop (they sync on activate / command).
    @tests (["../../packages/analytical/test/workspace-index.test.ts"])
    @status done
}

list_all_ideas {
    The extension should provide a command to list all ideas in the workspace with file location and summary.
}

index_comment_reference_inclusion {
    Comment `rq:[…]` sites are indexed as `comment_link` edges plus first-class code-file documents, not a separate idea kind.
    Graph surfaces them as idea → external file nodes (same file-node id as outbound [file_reference]).
    Live editor links still use [comment_grammar] / the comment resolver; the index path is native ["../indexer/indexer.rq".index_code_files].
    @status done
}

file_related_requirements {
    The extension should provide a view or command to get requirements relating to the currently open file.
    File-related lookup should follow both explicit file references and comment rq: links in that file.
    Comment links are read from indexed `comment_link` edges (["../indexer/indexer.rq".index_code_files]), not only the open buffer.
    @tests (
        ["../../crates/reqlan-analytical/tests/file_context.rs:file_context_includes_indexed_comment_links"]
        ["../../crates/reqlan-index/tests/code_file_index.rs:indexes_comment_references_from_non_rq_files"]
    )
}

deprecation_impact_analysis {
    The extension should analyse the impact of deprecated ideas on dependent requirements.
    Deprecation analysis should report downstream ideas that reference or depend on deprecated items.
}

git_dates {
    The extension should surface creation and last-modified dates, and change count, for ideas via
    git history where available.
    Values are written into the ideas index by the `git_dates` analyser as a silent background job after
    soft-sync / catalog updates (batched, yielded) — not triggered by Activity Bar focus, opening Timeline,
    or Ideas table load.
    Background queue prefers ideas in the active `.rq` editor file before the rest of the backlog.
    Consumers (Activity Bar signals, Timeline, Ideas table, future inlays) read indexed columns only.
    Detail and host wiring: ["git-codelens.rq".git_idea_timeline_analysis] and
    ["git-codelens.rq".git_dates_background_indexing].
    Implemented by ["../../packages/analytical/src/analysis/git-dates-analyser.ts"],
    ["../../packages/analytical/src/index-store/sqlite-store.ts"],
    ["../../packages/extension/src/extension/git-dates-background.ts"], and
    ["../../packages/extension/src/extension/register-git-dates-background.ts"].
    @status done
    @tests (
        ["../../packages/analytical/test/idea-timeline.test.ts"]
        ["../../packages/analytical/test/idea-timeline.test.ts:listIdeaIdsMissingGitDates can filter or prefer a file"]
        ["../../packages/analytical/test/idea-timeline.test.ts:listIdeaIdsMissingGitDates includes ideas missing change count"]
        ["../../packages/extension/test/git-dates-background.test.ts"]
        ["../../packages/extension/test/git-dates-background.test.ts:processes active-file priority ids before the global backlog"]
        ["../../packages/extension/test/git-dates-background.test.ts:stops early when shouldStop flips so a new file can be prioritised"]
    )
}

completion_tracking {
    The extension should track and surface completion, outstanding work, and open issues across the requirement graph.
    Completion tracking should derive from @status, @tags, and other lifecycle attributes where present.
}

local_graph_analysis {
    The extension should provide a function to access a small slice of the graph around a given idea.
}

semantic_analysis {
    Search graph for ideas related to a given idea or ideaset.
}