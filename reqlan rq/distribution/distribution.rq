import "@/reqlan rq/phonebook.rq" as phonebook_reqs

stopgap {
  Stopgap distribution is at https://marketplace.visualstudio.com/manage/publishers/reqlan
}

version_management {
  Package versions are managed with Changesets via ["../../.changeset/config.json"].
  Publishable channels (each tag-driven):
  - Marketplace/Open VSX: ["./distribution.rq".extension_package] (private npm package; tagged `ext/v*`)
  - GitHub Pages: ["./distribution.rq".site_package] (tagged `site/v*`)
  - npm: `@reqlan/language`, `@reqlan/analytical`, `@reqlan/cli`, and `@reqlan/mcp` (tagged `language/v*`, `analytical/v*`, `cli/v*`, `mcp/v*`)
  `reqlan-extension` has its own semver. It is not version-locked to `@reqlan/analytical`. The extension can update independently of the analytical surfaces it uses ([extension_package]).
  The version of `@reqlan/analytical` is the single source of truth for every `@reqlan/analytical-<platform>-<arch>` package ([rust_binary_distribution]). Platform packages are in the Changesets `ignore` list (`@reqlan/analytical-*`). Git `@reqlan/analytical` does not list them as `optionalDependencies`, so `changeset version` can run. After `changeset version`, ["../../scripts/prepare-native-packages.mjs"] copies the analytical version onto each platform package. npm publish writes pinned `optionalDependencies` with `--publish-versions`.
  Platform native packages are not a separate tag channel; they ride `analytical/v*` ([rust_binary_distribution]).
  VSIX packaging uses the package.json versions in that git commit ([vsix_native_not_from_npm]).
  `@reqlan/language`, `@reqlan/cli`, `@reqlan/mcp`, and the site keep their own versions.
  Contributors record release intent by adding a changeset file when a publishable package changes.
  @status done
  @tests (
    ["../../packages/analytical/test/native-packaging.test.ts"]
    ["../../packages/analytical/test/native-packaging.test.ts:analytical version is SSOT for platform packages; Changesets ignores them"]
    ["../../packages/analytical/test/native-packaging.test.ts:@reqlan/analytical optionalDependencies are publish-only, not in git"]
  )
}

npm_distribution {
  npm publishing runs only when a `language/v*`, `analytical/v*`, `cli/v*`, or `mcp/v*` tag is pushed.
  Tags are created by ["./distribution.rq".deployment_flow]; the npm workflow does not publish on ordinary commits.
  The deploy workflow builds language, analytical, cli, and mcp once.
  It then publishes each package in sequence with `pnpm --filter <pkg> publish --access public --no-git-checks`.
  The sequence is language, then all platform packages for [native_targets], then `@reqlan/analytical`, then cli, then mcp.
  The workflow does not publish `@reqlan/analytical` until every platform package of that `@reqlan/analytical` version is on the npm registry or was published in the same job ([rust_binary_distribution]).
  ["../../scripts/require-natives-published.mjs"] enforces that gate from the analytical package.json version (not Changesets).
  Before each package publishes, ["../../scripts/skip-already-published.mjs"] with `--filter <pkg>` checks the public npm registry and skips that step when the version is already published (sets `should_publish=false`). That avoids re-attempting already-published versions under OIDC.
  Auth uses npm trusted publishing (OIDC) from GitHub Actions — no long-lived `NPM_TOKEN`.
  Workflow requirements: `permissions.id-token: write`, Node ≥ 22.14 with npm ≥ 11.5.1, registry `https://registry.npmjs.org`.
  Each of `@reqlan/language`, `@reqlan/analytical`, `@reqlan/cli`, `@reqlan/mcp`, and every `@reqlan/analytical-<platform>-<arch>` platform package must have a Trusted Publisher on npmjs.com for GitHub Actions with org/user `littletuna4`, repository `reqlan`, workflow filename `deploy-npm.yml`, allowed action `npm publish`.
  Package `repository.url` must match `https://github.com/littletuna4/reqlan.git`. Provenance attestations are generated automatically for public packages from this public repo.
  Bootstrap: first publish of each scoped package can be done locally (you must be logged in to npm as an `@reqlan` org member) with `pnpm --filter @reqlan/language publish --access public --no-git-checks`, then platform packages, then analytical, then cli, then mcp — or wait for the trusted-publisher CI path after packages exist and Trusted Publisher is configured.
  Note: Packages must be `private: true` to skip local `changeset publish` (`pnpm run release:npm`), as with `reqlan-extension`. Platform packages are in the Changesets `ignore` list (`@reqlan/analytical-*`). Git `@reqlan/analytical` does not list them as `optionalDependencies`. ["../../scripts/prepare-native-packages.mjs"] `--publish-versions` writes pinned optionalDependencies for the npm checkout only. CI publishes platform packages per-package after binaries are built and does not use `changeset publish` for those tarballs.
  Published packages declare `publishConfig.access: public`.
   Trusted Publisher is configured on npmjs.com for each `@reqlan/analytical-<platform>-<arch>` package
  @implementation (
    ["../../.github/workflows/deploy-npm.yml"]
    ["../../scripts/skip-already-published.mjs"]
    ["../../scripts/require-natives-published.mjs"]
    ["../../scripts/prepare-native-packages.mjs"]
    ["../../scripts/collect-napi-binary.mjs"]
    ["../../package.json"]
  )
  @status in-progress
  @tests (
    ["../../packages/analytical/test/native-packaging.test.ts"]
    ["../../packages/analytical/test/native-packaging.test.ts:deploy-npm publishes platform packages before @reqlan/analytical"]
    ["../../packages/analytical/test/native-packaging.test.ts:analytical version is SSOT for platform packages; Changesets ignores them"]
    ["../../packages/analytical/test/native-packaging.test.ts:@reqlan/analytical optionalDependencies are publish-only, not in git"]
    ["../../packages/analytical/test/require-natives-published.test.ts"]
  )
}

native_targets {
  Canonical VS Code `targetPlatform` / npm `os`+`cpu` tuples for native distribution:
  - `win32-x64`
  - `win32-arm64`
  - `linux-x64`
  - `linux-arm64`
  - `darwin-x64`
  - `darwin-arm64`
  napi-rs package suffixes map as: `linux-*-gnu`, `darwin-*`, `win32-*-msvc` (gnu-only for linux; musl/alpine out of scope for the first matrix).
  Azure `build_native` uses the same tuples and `ciImage` from ["../../scripts/native-targets.mjs"].
  Used by [rust_binary_distribution], [vsix_export], [extension_host_target], and ["./native_host_binary.rq".native_host_binary].
  Implementation: ["../../scripts/native-targets.mjs"].
  @status done
}

rust_binary_distribution {
  One platform-specific npm package per [native_targets] tuple: `@reqlan/analytical-<platform>-<arch>` (napi suffixes: `linux-x64-gnu`, `linux-arm64-gnu`, `darwin-x64`, `darwin-arm64`, `win32-x64-msvc`, `win32-arm64-msvc`).
  Each package declares `os` and `cpu`, contains the matching core engine `.node`, and uses the `@reqlan/analytical` package.json version as the single source of truth. ["../../scripts/prepare-native-packages.mjs"] writes that version. Changesets ignores `@reqlan/analytical-*` in ["../../.changeset/config.json"]. Git `@reqlan/analytical` does not list platform packages as `optionalDependencies`; the published tarball does, pinned to the analytical version (`--publish-versions`) ([version_management]).
  CLI and MCP install the host-matching binary from those published optionalDependencies.
  Published on the existing `analytical/v*` channel ([npm_distribution] / [release_tagging]); not a separate tag channel.
  Website download listing and GitHub-release postinstall are later follow-ups (out of scope here).
  Engine port: ["../core_analysis/rust_port.rq".analytical_rust_port] / ["../core_analysis/rust_port.rq".native_bridge].
  Per-target VSIX packaging uses the same package layout under ["../../packages/analytical-native/"], filled by Azure from this git commit ([vsix_export], [vsix_native_not_from_npm]). npm tarballs of those packages are for CLI/MCP only.
  Implementation: ["../../packages/analytical-native/"], ["../../scripts/prepare-native-packages.mjs"], ["../../scripts/require-natives-published.mjs"], ["../../.changeset/config.json"], ["../../.github/workflows/deploy-npm.yml"] build-native job, ["../../azure-pipelines.yml"] build_native job.
  @status in-progress
  @todo first analytical/v* release with platform packages not yet published to npm
  @todo Trusted Publisher registration for platform packages on npmjs.com
  @tests (
    ["../../packages/analytical/test/load-native.test.ts"]
    ["../../packages/analytical/test/native-packaging.test.ts"]
    ["../../packages/analytical/test/native-packaging.test.ts:deploy-npm publishes platform packages before @reqlan/analytical"]
    ["../../packages/analytical/test/native-packaging.test.ts:analytical version is SSOT for platform packages; Changesets ignores them"]
    ["../../packages/analytical/test/native-packaging.test.ts:@reqlan/analytical optionalDependencies are publish-only, not in git"]
    ["../../packages/analytical/test/require-natives-published.test.ts"]
  )
  @plan {
    goal
      Ship per-architecture core engine binaries beside `@reqlan/analytical` so CLI, MCP, and per-target VSIXes can load the Rust engine for the extension-host platform.

    constraints
      - optionalDependencies (not devDependencies) for runtime consumers.
      - VSIX binaries come from Azure `build_native` on the `ext/v*` git tag, not from npm.
      - Trusted publishing and `analytical/v*` tagging stay [npm_distribution] / [release_tagging] for CLI/MCP.
      - gnu-only linux; musl and GitHub-release postinstall out of scope.
      - Website binary listing deferred.
      - Do not publish `@reqlan/analytical` until every platform package of that version is deployed ([npm_distribution]).

    steps
      1. Scaffold six platform packages with os/cpu and the napi binary. (done)
      2. Wire optionalDependencies on published `@reqlan/analytical` only; git omits them so Changesets can ignore platform packages. (done)
      3. Cross-compile reqlan-napi in CI for [native_targets]. (done in deploy-npm.yml and azure-pipelines.yml)
      4. Publish platform packages on `analytical/v*` before `@reqlan/analytical`.
      5. Prefer optionalDependency resolution in the loader; keep crates/target fallback for local dev by walking to repo-root `crates/Cargo.toml` (not `packages/crates` from `out/native`). (done)
  }
}

extension_host_target {
  The extension host must load the native core engine for its machine.
  The selection strategy is ["./native_host_binary.rq".native_host_binary].
  IndexService and WorkspaceIndex use `NativeWorkspaceIndex` (["../core_analysis/rust_port.rq".native_bridge]).
  If the engine is missing, activation fails.
  Loader: ["../../packages/analytical/src/native/load-native.ts"].
  The extension registers search folders from ["../../packages/extension/src/analytical_submodule/index.ts"].
  @status in-progress
  @todo first `@reqlan/analytical-<platform>-<arch>` release not yet published
  @tests (
    ["../../packages/analytical/test/load-native.test.ts"]
    ["../../packages/analytical/test/native-packaging.test.ts"]
  )
}

extension_package {
  The VS Code extension lives in packages/extension with npm package name `reqlan-extension` (Marketplace / Open VSX id `reqlan.reqlan-extension`).
  Its package.json version is the Marketplace and Open VSX release version. That version is independent of `@reqlan/analytical` ([version_management]).
  @status done
  @tests (
    ["../../packages/analytical/test/native-packaging.test.ts:analytical version is SSOT for platform packages; Changesets ignores them"]
  )
}

site_package {
  The static site lives in site/ with npm package name `site`.
  Its package.json version identifies site releases for tagging.
}

deployment_flow {
  On push to main, the release workflow first completes ["./distribution.rq".ci_gate].
  If pending changesets remain, it then applies them with `pnpm changeset version`.
  This bumps package.json for each affected package and writes changelogs.
  ["../../scripts/prepare-native-packages.mjs"] then copies the `@reqlan/analytical` version onto each platform package ([version_management]).
  The workflow commits the result, creates annotated tags for every bumped publishable package, and pushes the commit and tags back to main.
  Release tags use `site/v{version}`, `ext/v{version}`, `language/v{version}`, `analytical/v{version}`, `cli/v{version}`, and `mcp/v{version}` where `{version}` is the semver from that package's package.json.
  Deployment is tag-driven for every channel per ["./distribution.rq".release_tagging]; after ["./distribution.rq".ci_gate], the release workflow only versions and tags.
  GitHub Actions pushes with the default `GITHUB_TOKEN` do not trigger other workflows, so site and npm deploy are also invoked directly from the release workflow.
  Optional `RELEASE_PAT` allows tag pushes to trigger downstream workflows for manual reruns and external CI.
  Recursive runs are prevented by skipping the release workflow for release commits.
  Tag patterns and downstream deploy workflows follow ["./distribution.rq".release_tagging].
  The implementation lives in ["../../.github/workflows/release.yml"].
}

ci_gate {
  Pull-request CI and the main release pipeline share one sequential workflow: ["../../.github/workflows/ci.yml"].
  ["./distribution.rq".deployment_flow] must not apply changesets or create tags until that workflow succeeds.
  Order in one job: install workspace packages, compile crates and TypeScript once, then crate tests, ["../core_analysis/check.rq".check], then JS/TS tests (["./distribution.rq".prerelease_tests], ["../core_analysis/check.rq".check_meta_implementation], ["../development/commit.rq".rust_fmt]).
  `@reqlan/*` packages come from this git checkout (`workspace:*`), not from the npm registry.
  A push to main does not start CI a second time as an independent workflow; ["../../.github/workflows/release.yml"] calls ["../../.github/workflows/ci.yml"] with `workflow_call`.
  The version-bump commit (`chore(release):`) skips the gate and the version job to prevent a recursive run.
  @status done
  @implementation (
    ["../../.github/workflows/release.yml"]
    ["../../.github/workflows/ci.yml"]
    ["../../scripts/assert-workspace-reqlan.mjs"]
  )
  @tests (
    ["../../packages/analytical/test/release-ci-gate.test.ts"]
    ["../../packages/analytical/test/release-ci-gate.test.ts:release calls sequential CI before versioning"]
    ["../../packages/analytical/test/release-ci-gate.test.ts:CI is one sequential pull-request job"]
    ["../../packages/analytical/test/release-ci-gate.test.ts:one build is reused by crate tests, check, and package tests"]
    ["../../packages/analytical/test/release-ci-gate.test.ts:reference check uses workspace packages not npm"]
  )
}

prerelease_tests {
  JS/TS package tests (`pnpm run test:js`) run after the shared TypeScript compile (`pnpm run build:tsc`) from this git checkout.
  GitHub CI runs them in the same job as crate tests and ["../core_analysis/check.rq".check], after that compile (["./distribution.rq".ci_gate]).
  This job runs on every pull request and on main before versioning.
  Host native addon is required because analytical and language tests load `reqlan-napi`.
  Azure runs `pnpm run test:js` before VSIX publish ([extension_distribution]).
  Local `pnpm run test` runs `test:js` then `test:rust` (["../core_analysis/rust_port.rq".rust_crate_layout]).
  The implementation lives in ["../../.github/workflows/ci.yml"]; ["../../.github/workflows/release.yml"] calls it with `workflow_call` as part of ["./distribution.rq".ci_gate].
  Related: ["../core_analysis/check.rq".check_meta_implementation]
  @status done
  @implementation (
    ["../../.github/workflows/ci.yml"]
    ["../../.github/workflows/release.yml"]
    ["../../package.json"]
  )
  @tests (
    ["../../packages/analytical/test/release-ci-gate.test.ts"]
    ["../../packages/analytical/test/release-ci-gate.test.ts:one build is reused by crate tests, check, and package tests"]
    ["../../packages/analytical/test/release-ci-gate.test.ts:pnpm test runs crate tests"]
    ["../../packages/analytical/test/release-ci-gate.test.ts:TypeScript compile generates Langium sources first"]
  )
}

release_tagging {
  Deployment is tag-driven rather than commit-driven for every publishable channel.
  Tags are created automatically by ["./distribution.rq".deployment_flow], not manually.
  Site deployments are triggered by Git tags matching `site/v*` via ["./distribution.rq".site_distribution].
  VS Code extension publishing is triggered by Azure DevOps tags matching `ext/v*` via ["./distribution.rq".extension_distribution] and ["./distribution.rq".openvsx_registry].
  npm publishing is triggered by Git tags matching `language/v*`, `analytical/v*`, `cli/v*`, or `mcp/v*` via ["./distribution.rq".npm_distribution].
  Platform native packages ride `analytical/v*` (same version as `@reqlan/analytical`); they are not a separate tag prefix.
  The tag suffix after the channel prefix (`site/v`, `ext/v`, `language/v`, `analytical/v`, `cli/v`, `mcp/v`) equals the package.json version at that tag.
}

npm_package_readme {
  Publishable npm packages (`@reqlan/language`, `@reqlan/analytical`, `@reqlan/cli`, `@reqlan/mcp`) ship README.md generated from each package's `README.template.md`.
  Pattern matches [extension_readme]: centered logo, package name, description, features, phonebook links, and Changesets changelog body.
  Generated by ["../../scripts/generate-npm-package-readmes.ts"] via root `pnpm run build:readme`.
  Contact, Discord, sponsorship, and repository links come from [phonebook_reqs.phonebook] via ["../../scripts/phonebook.ts"] per [phonebook_reqs.npm_package_readme_links].
}

extension_readme {
  The VSIX package must include a README for the Marketplace and Open VSX listings.
  Contact, Discord, sponsorship, and repository links come from [phonebook_reqs.phonebook] via ["../../scripts/phonebook.ts"] per [phonebook_reqs.extension_readme_links].
  Release notes are appended from ["../../packages/extension/CHANGELOG.md"], which Changesets maintains per [version_management].
  Generated during extension build from ["../../packages/extension/README.template.md"] by ["../../packages/extension/scripts/generate-readme.ts"] per ["../development/build.rq".extension_readme_build].
}

root_readme {
  The GitHub repository README is the primary repo lander per ["./landers.rq".landers].
  Site, marketplace, repository, Discord, sponsorship, and contact links come from [phonebook_reqs.phonebook] via ["../../scripts/phonebook.ts"] per [phonebook_reqs.root_readme_links].
  Generated from ["../../README.template.md"] by ["../../scripts/generate-root-readme.ts"].
}

vsix_native_not_from_npm {
  Azure must not use the npm deploy path as an input when it packs VSIXes.
  Git at the `ext/v*` tag is canonical. Azure compiles the [native_targets] matrix from that commit ([vsix_export], [extension_distribution]).
  npm stays the install path for CLI and MCP only ([npm_distribution], [rust_binary_distribution]).
  @status done
  @tests (
    ["../../packages/analytical/test/native-packaging.test.ts"]
  )
  @justification {
    Marketplace version is `reqlan-extension` package.json and can differ from `@reqlan/analytical` ([version_management]).
    Azure compiles the six host binaries from that `ext/v*` commit (`build_native` in ["../../azure-pipelines.yml"]).
    The VSIX then contains the engine that matches the extension checkout, not a different `@reqlan/analytical` npm tarball.
  }
  @negative {
    Do not feed [npm_distribution] into Marketplace or Open VSX packaging.
    Do not wait for an `analytical/v*` tag, and do not poll `npm view` or `npm pack` (`scripts/fetch-native-packages.mjs --latest`).
    That path couples two tag channels and two clocks.
    Changesets can bump `@reqlan/analytical` and `reqlan-extension` independently ([version_management]).
    `--latest` then packs a different `@reqlan/analytical` semver than the `ext/v*` tag, or the pipeline waits on packages that are not on the registry yet.
  }
  @history {
    First native VSIX design: GitHub Actions compiled napi on `analytical/v*`, published platform packages, and Azure fetched `--latest` from npm.
    Azure did not compile Rust.
    That design treated npm as a hand-off between the two pipelines.
    After a changeset release, platform packages stayed on the previous patch (received `1.11.0`) while analytical and the extension moved (expected `1.11.1`).
    ["../../packages/analytical/test/native-packaging.test.ts"] failed on that mismatch.
    The lag is structural: `changeset version`, a glob in the Changesets `fixed` group, and a registry wait are not one atomic git snapshot.
    Decision: keep npm for CLI and MCP; compile the matrix in Azure from git for VSIX.
  }
}

vsix_export {
  One VSIX is built per [native_targets] tuple via `vsce package --target <tuple> --no-dependencies`.
  Each VSIX keeps today's esbuild-bundled JS (TS dependency graph stays bundled); only the matching platform native package is staged into the VSIX so other targets' binaries are excluded.
  Stamp `targetPlatform` so Marketplace/Open VSX route by extension host ([extension_host_target], ["./native_host_binary.rq".native_host_binary_distributed]).
  Artifact name shape: `reqlan-extension-<version>-<target>.vsix`.
  The VSIX includes logo assets from the extension media build: PNG for marketplace icon, language file icon, activity bar, and README; WebP as a raster fallback.
  Implementation: ["../../scripts/package-extension-targets.mjs"], ["../../scripts/prepare-native-packages.mjs"], ["../../scripts/collect-napi-binary.mjs"], ["../../packages/extension/.vscodeignore"] (`native/`), ["../../azure-pipelines.yml"].
  @status in-progress
  @todo first ext/v* release with six target VSIXes not yet shipped
  @plan {
    goal
      Ship six host-targeted VSIXes, each containing only the core engine `.node` for that extension-host platform.

    constraints
      - Keep --no-dependencies for the bundled TS graph; stage only the matching native package.
      - Git at the `ext/v*` tag is canonical ([vsix_native_not_from_npm]). Azure compiles the [native_targets] matrix from that commit and packs those binaries.
      - npm platform packages remain a CLI/MCP install path on `analytical/v*` ([npm_distribution] / [rust_binary_distribution]). They are not an input to Azure.
      - `vscode:prepublish` must not restage the host native addon (`--skip-native`); each VSIX `native/target.json` must equal that VSIX `--target`.

    steps
      1. Stage matching `@reqlan/analytical-<platform>-<arch>` into extension `native/` before vsce. (done)
      2. Package with `vsce package --target <tuple> --no-dependencies` for each [native_targets] entry; skip native restage in vscode:prepublish; verify packed `native/target.json`. (done)
      3. Compile the [native_targets] matrix in Azure and prepare packages from git versions + those artifacts. (done)
      4. Publish each target to Marketplace and Open VSX ([extension_distribution], [openvsx_registry]).
  }
}

site_distribution {
  GitHub Pages deployment runs only when an `site/v*` tag is pushed.
  Tags are created by ["./distribution.rq".deployment_flow]; the site workflow does not deploy on ordinary commits.
  The static build embeds `/spec` via ["../site/site.rq".spec_html_export], which needs the host `reqlan-napi` binary.
  The site workflow therefore builds and stages the host native engine (`scripts/stage-host-native.mjs` with `REQLAN_FORCE_NATIVE_BUILD=1`) before `pnpm run build`.
  The implementation lives in ["../../.github/workflows/deploy-site.yml"].
  @tests (
    ["../../packages/analytical/test/native-packaging.test.ts"]
  )
}

extension_distribution {
  Azure DevOps builds, tests, packages, and publishes the VS Code extension only for tags matching `ext/v*`.
  Tags are created by ["./distribution.rq".deployment_flow].
  The extension version comes from packages/extension/package.json at the tag; the pipeline must not overwrite it from the tag suffix.
  The implementation lives in ["../../azure-pipelines.yml"].
  The pipeline builds language, packages, and the extension only; it does not run the site static build or install Playwright ([site_distribution] owns site CI).
  Native core-engine binaries for VSIX come from the Azure `build_native` matrix on this git tag ([vsix_native_not_from_npm], [rust_binary_distribution], [vsix_export]). GitHub Actions still builds and publishes platform packages on `analytical/v*` for CLI/MCP ([npm_distribution]).
  Before tests Azure stages matrix artifacts into workspace packages (`prepare-native-packages.mjs --binary-dir`) and checks the host optionalDependency (`scripts/ensure-host-native.mjs --no-build`).
  VSIX packaging follows ["./distribution.rq".vsix_export]: one VSIX per [native_targets] tuple; Marketplace selects the VSIX matching the extension-host target ([extension_host_target]).
  Marketplace publish is complemented by Open VSX per ["./distribution.rq".openvsx_registry].
  @status in-progress
  @todo first multi-target Marketplace publish not yet run
}

openvsx_registry {
  The extension should be published to the Open VSX registry at https://open-vsx.org so it is installable in editors that use Open VSX instead of the Visual Studio Marketplace.
  Publishing should use the same tag-driven `ext/v*` releases as ["./distribution.rq".extension_distribution].
  Each per-target VSIX from ["./distribution.rq".vsix_export] is published to Open VSX (Marketplace and Open VSX both select by extension-host `targetPlatform`).
  Azure DevOps is the authoritative publish workflow; Open VSX publish belongs in ["../../azure-pipelines.yml"] alongside marketplace deploy.
  @status in-progress
  @todo first multi-target Open VSX publish not yet run
}

workflow_deduplication {
  Extension publish automation should have a single authoritative workflow to avoid duplicate marketplace releases.
  Azure DevOps is the authoritative extension publishing workflow per ["./distribution.rq".extension_distribution] and ["./distribution.rq".openvsx_registry].
  The site remains deployed by GitHub Actions per ["./distribution.rq".site_distribution].
  npm packages are published by GitHub Actions per ["./distribution.rq".npm_distribution].
  Versioning and tag creation are owned by GitHub Actions per ["./distribution.rq".deployment_flow].
}


grammar_linguist {
 The grammar should be updated to use
 https://github.com/github-linguist/linguist
 Acceptance is dependent on maturity and usage.
 (this is what gh uses for language rendering etc)
 @status pending
}
